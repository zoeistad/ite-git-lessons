# NOTE 
This is the source repo for working with git 

```bash 
git init 
git config --global init.defaultBranch main|master 

# check status and check the current branch 
git status 
git branch --show-current 


# only run it , if you using git for the first time. 
git config --global user.email "zoeistad@example.com"
git config --global user.name "zoe-istad"

# to keep track of your code 
# 1. Working Directory 
git add filename 
git add . # (all files / folders inside current directory)

# 2. Staging Area 
```